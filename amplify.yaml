version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No preBuild commands"
    build:
      commands:
        - echo "Building static site"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []